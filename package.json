{
  "name": "move-issues",
  "version": "1.0.0",
  "description": "Moves the given `issues` that are in the given `from_column` to the given `to_column`. The `issues` can be in multiple columns, but only the ones in the `from_column` will be moved.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bjthompson805/move-issues.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/bjthompson805/move-issues/issues"
  },
  "homepage": "https://github.com/bjthompson805/move-issues#readme",
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@actions/github": "^4.0.0"
  }
}
